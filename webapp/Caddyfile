# Caddy config for the word-game Mini App API on the VPS.
# Caddy auto-provisions and renews TLS via Let's Encrypt for this hostname, then
# reverse-proxies to the in-process API server (WEBAPP_API_PORT in .env, default 8081).
#
# Install on the VPS (Debian/Ubuntu):
#   sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https curl
#   curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
#   curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' | sudo tee /etc/apt/sources.list.d/caddy-stable.list
#   sudo apt update && sudo apt install caddy
# Then put this file at /etc/caddy/Caddyfile and: sudo systemctl reload caddy
#
# DNS prerequisite: an A record  api.wellversed.live → <VPS public IP>.

api.wellversed.live {
	reverse_proxy localhost:8081
}
